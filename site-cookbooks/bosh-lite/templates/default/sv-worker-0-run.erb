#!/bin/bash -l

PATH=/opt/rbenv/shims:/opt/rbenv/bin:/opt/rbenv/bin/rbenv:/bin:/usr/bin
mkdir -p /etc/sv/worker-0/env

export QUEUE='*'

exec 2>&1
exec chpst -e /etc/sv/worker-0/env -u <%= @options[:user] %> bosh-director-worker -i 0 -c /opt/bosh/config/director.yml
