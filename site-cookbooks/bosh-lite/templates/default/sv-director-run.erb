#!/bin/sh

PATH=/opt/rbenv/shims:/opt/rbenv/bin:/opt/rbenv/bin/rbenv:/bin:/usr/bin
mkdir -p /etc/sv/director/env

exec 2>&1
exec chpst -e /etc/sv/director/env -u <%= @options[:user] %> bosh-director -c /opt/bosh/config/director.yml
